import React, { ComponentType } from 'react';
import { DeviceInfo } from '../utils/withDevice';
import { RenderRuntime } from '../typings/runtime';
declare type Props = {
    runtime: RenderRuntime;
    deviceInfo: DeviceInfo;
};
declare const withPerformanceMeasures: <P extends Props>(Component: React.ComponentType<P>) => ({ ...props }: P) => JSX.Element;
export { withPerformanceMeasures };
